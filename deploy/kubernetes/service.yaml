apiVersion: v1
kind: Service
metadata:
  name: exo
  namespace: exo
  labels:
    app.kubernetes.io/name: exo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: exo
  ports:
    - name: http
      port: 8293
      targetPort: http
